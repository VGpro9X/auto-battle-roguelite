(function(root){
'use strict';
function query(){try{return typeof location!=='undefined'?new URLSearchParams(location.search):null;}catch(_){return null;}}
function requestedMode(){const q=query(),value=q&&q.get('duelRenderer');if(value==='vector'||value==='v2'||value==='v3')return value;return root.DUEL_RENDERER_V3_DEFAULT===true?'v3':'v2';}
function requestedQuality(){const q=query(),value=(q&&q.get('visualQuality'))||root.DUEL_RENDERER_V3_QUALITY||'balanced';return ['full','balanced','low'].includes(String(value).toLowerCase())?String(value).toLowerCase():'balanced';}
function reducedMotion(){try{return typeof matchMedia==='function'&&matchMedia('(prefers-reduced-motion: reduce)').matches;}catch(_){return false;}}
function loadImage(src,label='asset'){return new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error('Failed to load V3 '+label+': '+src));image.src=src;});}
function normalizeState(fighter){return typeof root.normalizeDuelVisualState==='function'?root.normalizeDuelVisualState(fighter&&fighter.action):(fighter&&fighter.action)||'idle';}
function createTransform(canvas,match){const rect=canvas.getBoundingClientRect(),width=Math.max(1,rect.width||canvas.width||1),height=Math.max(1,rect.height||canvas.height||1),arena=match.arena,scale=Math.min(width/arena.width,height/arena.height),ox=(width-arena.width*scale)/2,oy=(height-arena.height*scale)/2;return{scale,ox,oy,width,height,centerX:arena.width/2,zoom:1,mobile:width<620,shakeX:0,shakeY:0,x:v=>ox+v*scale,y:v=>oy+v*scale};}
function createBridge(canvas){
  if(typeof root.createDuelRendererV2!=='function')throw new Error('Renderer V2 unavailable');
  const fallback=root.createDuelRendererV2(canvas),api=root.AutoBattleRendererV3,ctx=canvas.getContext('2d'),images=new Map(),arenaImages=new Map(),lastV3Frames=new Map(),stateClocks=new Map(),lightFlashes=[];
  if(!api||typeof api.create!=='function'){canvas.dataset.duelRenderer='v2-v3-api-missing';return fallback;}
  const quality=requestedQuality(),motionReduced=reducedMotion(),budget=quality==='full'?{projectiles:10,flash:8,alpha:1}:quality==='balanced'?{projectiles:6,flash:5,alpha:.78}:{projectiles:3,flash:2,alpha:.52},coreVfx=typeof root.createDuelVfxV3==='function'?root.createDuelVfxV3({quality,reducedMotion:motionReduced}):null;
  const meta=api.create({enabled:true,quality});let status={enabled:true,ready:false,arenaReady:false,quality,reason:'initializing'},activeStates=[],arenaActive=false,arenaLoadError=null;
  async function preloadProductionStates(){activeStates=[];for(const stateId of api.REQUIRED_STATES||[]){const resolved=meta.resolveFighterState(stateId);if(resolved.renderer!=='v3'||!resolved.entry||!resolved.entry.src)continue;const image=await loadImage(resolved.entry.src,'fighter asset');images.set(resolved.entry.src,image);activeStates.push(stateId);}}
  async function preloadProductionArena(){arenaActive=false;arenaLoadError=null;arenaImages.clear();const resolved=typeof meta.resolveArena==='function'?meta.resolveArena():{renderer:'v2',reason:'arena resolver unavailable'};if(resolved.renderer!=='v3'||!Array.isArray(resolved.layers))return false;try{const loaded=await Promise.all(resolved.layers.map(async layer=>[layer.src,await loadImage(layer.src,'arena asset')]));for(const [src,image] of loaded)arenaImages.set(src,image);arenaActive=resolved.layers.every(layer=>arenaImages.has(layer.src));if(!arenaActive){arenaImages.clear();return false;}return true;}catch(error){arenaLoadError=error&&error.message||'arena preload failed';arenaImages.clear();arenaActive=false;return false;}}
  const preload=Promise.resolve(fallback.preload).then(()=>meta.initialize()).then(async next=>{status=next;if(!next.ready){canvas.dataset.duelRenderer='v2-v3-fallback';return next;}await preloadProductionStates();await preloadProductionArena();canvas.dataset.duelRenderer=arenaActive||activeStates.length?'v3-partial':'v3-foundation';return next;}).catch(error=>{status={enabled:true,ready:false,arenaReady:false,quality,reason:error&&error.message||'bridge-failed'};arenaActive=false;arenaImages.clear();canvas.dataset.duelRenderer='v2-v3-fallback';return status;});
  function touchState(fighter,round){if(!fighter)return'idle';const stateId=normalizeState(fighter),side=fighter.side||'unknown',now=Math.max(0,Number(round&&round.time)||0),prev=stateClocks.get(side);if(!prev||prev.fighter!==fighter||prev.stateId!==stateId||now<prev.enteredAt)stateClocks.set(side,{fighter,stateId,enteredAt:now});return stateId;}
  function resolveV3(fighter,stateId){if(!status.ready)return null;const resolved=meta.resolveFighterState(stateId);if(resolved.renderer!=='v3'||!resolved.entry)return null;const image=images.get(resolved.entry.src);return image?{stateId,entry:resolved.entry,image}:null;}
  function frameIndex(entry,round,fighter,stateId){const count=Math.max(1,Number(entry.frameCount)||1),fps=Math.max(1,Number(entry.fps)||1),clock=stateClocks.get(fighter&&fighter.side),now=Math.max(0,Number(round&&round.time)||0),elapsed=Math.max(0,now-Number(clock&&clock.stateId===stateId?clock.enteredAt:now));return entry.loop===false?Math.min(count-1,Math.floor(elapsed*fps)):Math.floor(elapsed*fps)%count;}
  function drawV3Arena(match,tr,foreground=false){if(!arenaActive||!status.ready||!match||!match.arena)return false;const resolved=meta.resolveArena();if(resolved.renderer!=='v3'||!Array.isArray(resolved.layers))return false;for(const layer of resolved.layers){if(Boolean(layer.foreground)!==foreground)continue;const image=arenaImages.get(layer.src);if(!image)return false;const lt=fallback.camera&&typeof fallback.camera.layerTransform==='function'?fallback.camera.layerTransform(tr,match.arena,layer.parallax):tr;const x=Number(layer.x||0),y=Number(layer.y||0),w=Number(layer.width||match.arena.width),h=Number(layer.height||match.arena.height);ctx.save();ctx.globalAlpha=Math.max(0,Math.min(1,Number(layer.opacity??1)));ctx.drawImage(image,lt.x(x),lt.y(y),w*lt.scale,h*lt.scale);ctx.restore();}return true;}
  function drawV3ShadowIdentity(fighter,tr){const x=tr.x(fighter.x),y=tr.y(fighter.y),s=tr.scale;ctx.save();ctx.fillStyle='rgba(0,0,0,.34)';ctx.beginPath();ctx.ellipse(x,y-2*s,34*s,8*s,0,0,Math.PI*2);ctx.fill();ctx.globalAlpha=.55;ctx.strokeStyle=fighter.side==='player'?'#38bdf8':'#fb7185';ctx.lineWidth=Math.max(1,2*s);ctx.beginPath();ctx.ellipse(x,y-3*s,40*s,10*s,0,0,Math.PI*2);ctx.stroke();ctx.restore();}
  function drawV3Fighter(fighter,round,tr,visual){const entry=visual.entry,index=frameIndex(entry,round,fighter,visual.stateId),fw=Number(entry.frameWidth||256),fh=Number(entry.frameHeight||256),columns=Math.max(1,Number(entry.columns||entry.frameCount||1)),sx=(index%columns)*fw,sy=Math.floor(index/columns)*fh,displayWorldWidth=Math.max(1,Number(entry.displayWorldWidth||176)),worldScale=displayWorldWidth/fw,anchors=entry.anchors&&entry.anchors[index],feet=anchors&&anchors.feet?anchors.feet:{x:fw/2,y:fh},dw=fw*worldScale*tr.scale,dh=fh*worldScale*tr.scale,x=tr.x(fighter.x),y=tr.y(fighter.y);drawV3ShadowIdentity(fighter,tr);ctx.save();ctx.translate(x,y);if((fighter.facing||1)<0)ctx.scale(-1,1);ctx.globalAlpha=fighter.hp<=0?.72:1;ctx.drawImage(visual.image,sx,sy,fw,fh,-feet.x*worldScale*tr.scale,-feet.y*worldScale*tr.scale,dw,dh);ctx.restore();lastV3Frames.set(fighter.side,{fighter,entry,index,worldScale});}
  function drawV3Attachments(fighter,round,tr){const chest=getAnchor(fighter,'chest'),feet=getAnchor(fighter,'feet'),cx=tr.x(chest.x),cy=tr.y(chest.y),fx=tr.x(feet.x),fy=tr.y(feet.y),s=tr.scale;if(fighter.shield>0){ctx.save();ctx.strokeStyle='rgba(125,211,252,.78)';ctx.lineWidth=Math.max(1,3*s);ctx.beginPath();ctx.arc(cx,cy,43*s,0,Math.PI*2);ctx.stroke();ctx.restore();}const rank=typeof root.getDuelSkillRank==='function'?root.getDuelSkillRank:null,frost=rank?rank(fighter.build,'frost'):0;if(frost){ctx.save();ctx.strokeStyle='rgba(147,197,253,.24)';ctx.lineWidth=Math.max(1,2*s);ctx.beginPath();ctx.arc(fx,fy-48*s,(52+frost*6)*s,0,Math.PI*2);ctx.stroke();ctx.restore();}const orbit=rank?rank(fighter.build,'orbit'):0;if(orbit){for(let i=0;i<orbit;i++){const a=round.time*3.1+i*Math.PI*2/orbit,bx=cx+Math.cos(a)*48*s,by=cy+10*s+Math.sin(a)*18*s;ctx.save();ctx.translate(bx,by);ctx.rotate(a);ctx.fillStyle='#e2e8f0';ctx.fillRect(-2*s,-11*s,4*s,22*s);ctx.restore();}}}
  function lightColor(source,type){const s=String(source||type||'').toLowerCase();if(s.includes('fire')||s.includes('burn'))return'255,120,55';if(s.includes('frost')||s.includes('ice'))return'125,205,255';if(s.includes('lightning'))return'230,235,255';if(s.includes('heal'))return'110,245,185';if(s.includes('shield')||s.includes('barrier'))return'100,205,255';return'235,215,175';}
  function consumeLighting(events){for(const event of events||[]){if(!event||!['hit','heal','shield_gain','ko'].includes(event.type))continue;if(lightFlashes.length>=budget.flash)lightFlashes.shift();lightFlashes.push({x:Number(event.x)||0,y:Number(event.y)||0,color:lightColor(event.source,event.type),life:event.type==='ko'?.24:.12,max:event.type==='ko'?.24:.12});}}
  function consume(events){const list=events||[];if(coreVfx){const owned=list.filter(event=>coreVfx.ownsEvent(event)),legacy=list.filter(event=>!coreVfx.ownsEvent(event));fallback.consume(legacy);if(owned.length){fallback.tierVfx?.consume?.(owned);fallback.camera?.consume?.(owned);coreVfx.consume(owned);}}else fallback.consume(list);consumeLighting(list);}
  function radial(x,y,r,a,color){const g=ctx.createRadialGradient(x,y,0,x,y,r);g.addColorStop(0,`rgba(${color},${a})`);g.addColorStop(1,`rgba(${color},0)`);ctx.fillStyle=g;ctx.fillRect(x-r,y-r,r*2,r*2);}
  function drawLighting(match,tr,dt){const round=match&&match.currentRound;if(!round)return;for(const f of lightFlashes)f.life-=Math.max(0,Number(dt)||0);while(lightFlashes.length&&lightFlashes[0].life<=0)lightFlashes.shift();ctx.save();ctx.globalCompositeOperation='screen';const projectiles=(round.projectiles||[]).slice(0,budget.projectiles);for(const p of projectiles){const x=tr.x(p.x),y=tr.y(p.y),radius=(motionReduced?24:30)*tr.scale;radial(x,y,radius,.16*budget.alpha,lightColor(p.source,p.effect));}for(const fighter of Object.values(round.fighters||{})){if(!fighter||fighter.hp<=0)continue;if(fighter.shield>0){const a=getAnchor(fighter,'chest');radial(tr.x(a.x),tr.y(a.y),52*tr.scale,.10*budget.alpha,'90,205,255');}}for(const flash of lightFlashes){const t=Math.max(0,flash.life/flash.max);radial(tr.x(flash.x),tr.y(flash.y),(motionReduced?44:58)*tr.scale,.22*t*budget.alpha,flash.color);}ctx.restore();const width=tr.width||canvas.getBoundingClientRect().width,height=tr.height||canvas.getBoundingClientRect().height;if(round.phase!=='NORMAL'){ctx.save();ctx.fillStyle=round.phase==='TỬ CHIẾN'?`rgba(115,0,18,${.11*budget.alpha})`:`rgba(90,10,20,${.065*budget.alpha})`;ctx.fillRect(0,0,width,height);ctx.restore();}const ratios=Object.values(round.fighters||{}).filter(Boolean).map(f=>Math.max(0,f.hp)/Math.max(1,f.maxHp||1));const low=ratios.length?Math.min(...ratios):1;if(low<.25){const strength=(.25-low)/.25;ctx.save();const g=ctx.createRadialGradient(width/2,height/2,Math.min(width,height)*.25,width/2,height/2,Math.max(width,height)*.72);g.addColorStop(0,'rgba(35,0,0,0)');g.addColorStop(1,`rgba(70,0,8,${(.14*strength*budget.alpha).toFixed(3)})`);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);ctx.restore();}}
  function render(match,dt){lastV3Frames.clear();coreVfx?.update?.(dt);const round=match&&match.currentRound,player=round&&round.fighters&&round.fighters.player,opponent=round&&round.fighters&&round.fighters.opponent,pState=touchState(player,round),oState=touchState(opponent,round),pVisual=player&&resolveV3(player,pState),oVisual=opponent&&resolveV3(opponent,oState),v3Sides=[];if(pVisual)v3Sides.push('player');if(oVisual)v3Sides.push('opponent');if(!round){fallback.render(match,dt);return;}const tr=typeof fallback.prepareFrameTransform==='function'?fallback.prepareFrameTransform(match,dt):(typeof fallback.getLastTransform==='function'&&fallback.getLastTransform())||createTransform(canvas,match);if(arenaActive){const rect=canvas.getBoundingClientRect(),width=Math.max(1,rect.width||canvas.width||1),height=Math.max(1,rect.height||canvas.height||1);fallback.resize();ctx.clearRect(0,0,width,height);drawV3Arena(match,tr,false);fallback.render(match,dt,{skipFighterSides:v3Sides,skipArena:true,preserveCanvas:true,transformOverride:tr});}else fallback.render(match,dt,{skipFighterSides:v3Sides,transformOverride:tr});if(pVisual){drawV3Fighter(player,round,tr,pVisual);drawV3Attachments(player,round,tr);}if(oVisual){drawV3Fighter(opponent,round,tr,oVisual);drawV3Attachments(opponent,round,tr);}coreVfx?.render?.(ctx,tr,match,getAnchor);if(arenaActive)drawV3Arena(match,tr,true);drawLighting(match,tr,dt);}
  function getAnchor(fighter,name){const tracked=lastV3Frames.get(fighter&&fighter.side);if(!tracked||tracked.fighter!==fighter)return fallback.getAnchor(fighter,name);const anchors=tracked.entry.anchors&&tracked.entry.anchors[tracked.index],anchor=anchors&&anchors[name],feet=anchors&&anchors.feet;if(!anchor||!feet)return fallback.getAnchor(fighter,name);const facing=fighter.facing||1,scale=tracked.worldScale;return{x:fighter.x+(anchor.x-feet.x)*scale*facing,y:fighter.y+(anchor.y-feet.y)*scale};}
  function getStatus(){const base=typeof fallback.getStatus==='function'?fallback.getStatus():{};return{mode:status.ready?(arenaActive||activeStates.length?'v3-partial':'v3-foundation'):'v2',v3:status,activeStates:activeStates.slice(),arena:{active:arenaActive,id:arenaActive?'ashen-sanctum-v3':null,error:arenaLoadError,loaded:[...arenaImages.keys()]},coreVfx:coreVfx?.getStatus?.()||{enabled:false,reason:'v3-vfx-module-missing'},lighting:{enabled:true,quality,motionReduced,projectileBudget:budget.projectiles,flashBudget:budget.flash,activeFlashes:lightFlashes.length},fallback:base};}
  return Object.assign({},fallback,{preload,consume,render,getAnchor,getStatus,v3:meta,coreVfx,fallback});
}
function createConfigured(canvas){const mode=requestedMode();if(mode==='vector'&&typeof root.createDuelVectorRenderer==='function'){const r=root.createDuelVectorRenderer(canvas);canvas.dataset.duelRenderer='vector';return r;}if(mode!=='v3')return root.createDuelRendererV2(canvas);try{return createBridge(canvas);}catch(error){console.warn('Renderer V3 bridge unavailable; Renderer V2 fallback active.',error);return root.createDuelRendererV2(canvas);}}
root.getDuelRendererV3Mode=requestedMode;root.createDuelRendererV3Bridge=createBridge;root.createConfiguredDuelRendererV3=createConfigured;
})(typeof window!=='undefined'?window:globalThis);